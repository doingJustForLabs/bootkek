import React from 'react';

const Post = () => {
    return (
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', backgroundColor: 'blue', width: '100%', minHeight: '300px' }}>
            ПОСТ
        </div>
    );
};

export default Post;